import React from 'react'

export const Main = ({children}) => {
  return (
    <main className=' h-full w-full flex-1 relative overflow-hidden flex justify-center items-center gap-[20px] '>{children}</main>
  )
}
